interface:
  display_name: "HTML Presentation Builder"
  short_description: "Create art-directed HTML decks with notes, WebP assets, and visual QA"
  default_prompt: "Use $build-html-slides to create or revise an art-directed HTML presentation with sibling Markdown presenter notes and a hash-bound sources.json image cache. Model the named or inferred audiences, their decisions, baseline knowledge, detail tolerance, and objections before storyboarding; let that model determine information order, decision timing, and when specialist depth appears instead of using a generic topic outline. For an ordinary existing-deck revision, default to Edit Only and disclose that it was not revalidated. Upgrade only when the user explicitly requests validation/testing/review or precision/final polish. Before Full Validation, run render_slides.js --check; if Playwright or Chromium is unavailable, report the error and ask before installing anything. Retain normal, short, and zoom150 evidence, but block vision on automated geometry failures, batch at most four slides per vision call, and inspect stress profiles only for critical or warning-routed slides. Add responsive profiles only when requested. For named fictional characters or real people, bind every meaningful image to a separate local canonical reference and cue-based identity review; never trust filenames, alt text, folders, captions, or source tags as identity evidence. Theme the template's icon-based previous/page-input/total/next/fullscreen panel without changing its structure. After edits, use --slides with the narrow text, image, navigation, or all change type so only changed slides and neighbors rerender. Reuse unchanged source hashes and render evidence, run --finalize when settled, calculate the quality score once, and then complete required final cross-reviews."
